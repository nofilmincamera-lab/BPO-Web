# BPO Intelligence Pipeline - ULTRA MINIMAL Dependencies
# ONLY packages that are DIRECTLY IMPORTED in source code
# Python 3.11+ required

# ============================================================================
# DIRECTLY IMPORTED PACKAGES ONLY
# ============================================================================

# API Framework (src/api/main.py imports fastapi)
fastapi==0.115.0
# Server to run FastAPI (not imported, but required to run uvicorn src.api.main:app)
uvicorn[standard]==0.31.1

# Orchestration (src/flows/extraction_flow.py, src/api/main.py)
prefect==3.1.9
prefect-docker==0.6.0

# Docker operations (required by prefect-docker)
docker==7.1.0

# Database (src/flows/extraction_flow.py)
asyncpg==0.29.0

# NLP (src/extraction/spacy_pipeline.py)
spacy[cuda121]==3.8.2

# GPU/ML (conditional import in spacy_pipeline.py for GPU)
# Install with: pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1

# Text Processing (scripts/preprocess.py)
tldextract==5.1.2
pybloom-live==4.0.0
ijson==3.4.0
tqdm==4.67.1

# ============================================================================
# REMOVED FROM "MINIMAL" (Not actually imported!)
# ============================================================================
# pydantic - Transitive dep of fastapi/prefect, don't pin directly
# pydantic-settings - NEVER IMPORTED, not needed!
# python-dotenv - NEVER IMPORTED, not needed!
# transformers - Transitive dep of spacy/torch, let pip resolve
# tokenizers - Transitive dep of transformers
# numpy - Transitive dep of spacy/torch
# torchvision - NEVER IMPORTED
# torchaudio - NEVER IMPORTED

# ============================================================================
# TOTAL: 12 DIRECT DEPENDENCIES
# ============================================================================
# All others are transitive dependencies managed by pip

